extends layout

block content
  h1 
  div
        button.w3-card.w3-button.w3-text-grey.w3-animate-zoom.w3-xlarge(onclick ="home( )") Home
  table.w3-table-all
    each atribute in DATA
      tr
        th= atribute.id
        td
          p.w3-xlarge= DATA[atribute.id]

      

